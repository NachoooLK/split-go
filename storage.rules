rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Permitir acceso temporal completo para resolver CORS
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
